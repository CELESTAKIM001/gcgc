import "server-only"; import { getDb } from './db';
export async function handleMpesaCallback(req:Request){try{const body=await req.json();const cb=body?.Body?.stkCallback;const db=await getDb();const checkout=cb?.CheckoutRequestID;const code=cb?.ResultCode;const items=cb?.CallbackMetadata?.Item||[];const meta:any={};for(const x of items)meta[x.Name]=x.Value;const payment=await db.collection('payments').findOne({checkoutRequestId:checkout});await db.collection('payments').updateOne({checkoutRequestId:checkout},{$set:{status:code===0?'paid':'failed',resultCode:code,resultDescription:cb?.ResultDesc,mpesaReceiptNumber:meta.MpesaReceiptNumber,transactionDate:meta.TransactionDate,amountConfirmed:meta.Amount,phoneConfirmed:meta.PhoneNumber,callbackReceivedAt:new Date(),updatedAt:new Date()}});if(code===0&&payment){await db.collection('leads').updateOne({_id:payment.leadId},{$set:{paymentStatus:'paid',paidAt:new Date(),updatedAt:new Date()}});await db.collection('notifications').insertOne({type:'PAYMENT_CONFIRMED',paymentId:payment._id,leadId:payment.leadId,memberId:payment.memberId,read:false,createdAt:new Date()});await db.collection('audit_logs').insertOne({action:'MPESA_PAYMENT_CONFIRMED',checkoutRequestId:checkout,receipt:meta.MpesaReceiptNumber,createdAt:new Date()})}return Response.json({ResultCode:0,ResultDesc:'Accepted'})}catch(e){console.error(e);return Response.json({ResultCode:0,ResultDesc:'Accepted'})}}
