'use client'; import {useState} from 'react'; import {Site} from '../site';
export default function Quote(){const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:String(f.title),description:String(f.description),location:String(f.location),estimatedValue:Number(f.estimatedValue)||0})});const j=await r.json();setMsg(r.ok?'Quote request submitted. Your lead is now awaiting GLDC administration approval.':j.error||'Unable to submit request.');setLoading(false);if(r.ok)(e.currentTarget as HTMLFormElement).reset()}return <Site><section className="page-hero"><div className="container"><span className="eyebrow">START YOUR PROJECT</span><h1>Request a quote.</h1><p>Submit your project details securely through your approved GLDC member account.</p></div></section><section className="section"><div className="container"><div className="card form-card"><form onSubmit={submit} className="form-grid"><div className="field full"><label>Project title</label><input name="title" required/></div><div className="field"><label>Location</label><input name="location" required/></div><div className="field"><label>Estimated project value (KES)</label><input name="estimatedValue" type="number" min="0"/></div><div className="field full"><label>Project description</label><textarea name="description" required placeholder="Tell us about the land, design, approvals or consultancy support you need."/></div><div className="field full"><button className="btn primary" disabled={loading}>{loading?'SUBMITTING…':'REQUEST A QUOTE'}</button></div></form>{msg&&<div className="notice">{msg}</div>}</div></div></section></Site>}
