'use client'; import {useState} from 'react'; import Link from 'next/link'; import {Site} from '../site';
export default function Login(){const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);const b=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const j=await r.json();if(r.ok)location.href=j.user.role==='admin'?'/admin':'/member';else setMsg(j.error||'Login failed')}catch{setMsg('Network error. Please try again.')}finally{setLoading(false)}}return <Site><section className="section"><div className="card form-card narrow"><span className="eyebrow">MEMBER PORTAL</span><h1>Sign in</h1><p>Access your GLDC account, project leads and approved payment stages.</p><form onSubmit={submit} className="form-grid"><div className="field full"><label>Email</label><input name="email" type="email" required autoComplete="email"/></div><div className="field full"><label>Password</label><input name="password" type="password" required autoComplete="current-password"/></div><div className="field full"><button className="btn primary" disabled={loading}>{loading?'SIGNING IN…':'SIGN IN'}</button></div></form>{msg&&<div className="notice">{msg}</div>}<p>New to GLDC? <Link className="link" href="/register">Create a member account</Link>.</p></div></section></Site>}
