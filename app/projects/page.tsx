import ContentPage from '../content-page'; import {projects} from '../site'; import Link from 'next/link';
export default function Projects(){return <ContentPage eyebrow="PORTFOLIO" title="Selected project stories."><div className="grid grid-3">{projects.map(p=><article className="card project-card hover" key={p.title}><img className="project-img" src={p.image} alt={p.title}/><div className="body"><div className="project-meta"><span>{p.location}</span><span>{p.type}</span></div><h3>{p.title}</h3><p>{p.text}</p><Link className="link" href="/quote">Enquire about a similar project →</Link></div></article>)}</div></ContentPage>}
