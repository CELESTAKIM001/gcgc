import { NextResponse } from 'next/server'; import { currentUser } from '@/lib/auth'; import { getDb } from '@/lib/db'; import { ObjectId } from 'mongodb';
export async function GET(){try{const me=await currentUser();if(!me)return NextResponse.json({error:'Unauthorized'},{status:401});const db=await getDb();const leads=await db.collection('leads').find({memberId:new ObjectId(me.id)}).sort({createdAt:-1}).toArray();return NextResponse.json(leads.map(l=>({...l,id:String(l._id),memberId:undefined,_id:undefined})))}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
export async function POST(req:Request){try{const me=await currentUser();if(!me||me.role!=='member')return NextResponse.json({error:'Member authentication required'},{status:403});const b=await req.json();const db=await getDb();const u=await db.collection('users').findOne({_id:new ObjectId(me.id),status:'approved',emailVerified:true});if(!u)return NextResponse.json({error:'Your member account must be approved before creating a lead.'},{status:403});const lead={memberId:u._id,title:String(b.title||'').trim(),description:String(b.description||'').trim(),location:String(b.location||'').trim(),estimatedValue:Number(b.estimatedValue)||0,status:'pending_admin_approval',paymentStatus:'unpaid',createdAt:new Date(),updatedAt:new Date()};if(!lead.title||!lead.description||!lead.location)return NextResponse.json({error:'Title, description and location are required.'},{status:400});const r=await db.collection('leads').insertOne(lead);await db.collection('notifications').insertOne({type:'NEW_MEMBER_LEAD',leadId:r.insertedId,memberId:u._id,read:false,createdAt:new Date()});return NextResponse.json({ok:true,id:String(r.insertedId),status:lead.status},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
