'use client'; import {useState} from 'react'; import Link from 'next/link'; import {Site} from '../site';
export default function Contact(){const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:`Consultation: ${f.name}`,description:f.message,location:f.location,estimatedValue:0})});const j=await r.json();setMsg(r.ok?'Your consultation request has been submitted. We will contact you after account approval.':j.error||'Please sign in as an approved member to submit a request.');setLoading(false)}return <Site><section className="page-hero"><div className="container"><span className="eyebrow">CONTACT GLDC</span><h1>Let’s discuss your project.</h1></div></section><section className="section"><div className="container grid grid-2"><div><h2>Get a consultation</h2><p>For production project enquiries, create or use your GLDC member account. Approved members can submit leads and progress them through the GLDC workflow.</p><Link className="btn primary" href="/register">BECOME A MEMBER</Link></div><div className="card form-card"><form onSubmit={submit} className="form-grid"><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Location</label><input name="location" required/></div><div className="field full"><label>Message</label><textarea name="message" required/></div><div className="field full"><button className="btn primary" disabled={loading}>{loading?'SUBMITTING…':'SEND CONSULTATION REQUEST'}</button></div></form>{msg&&<div className="notice">{msg}</div>}</div></div></section></Site>}
