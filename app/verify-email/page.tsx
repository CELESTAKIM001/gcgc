'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {Site} from '../site';
export default function VerifyEmail(){const [msg,setMsg]=useState('Verifying…');useEffect(()=>{fetch('/api/auth/verify-email'+location.search).then(r=>r.json()).then(j=>setMsg(j.message||j.error||'Verification complete')).catch(()=>setMsg('Verification failed.'))},[]);return <Site><section className="section"><div className="card form-card narrow"><span className="eyebrow">ACCOUNT</span><h1>Email verification</h1><p>{msg}</p><Link className="btn primary" href="/login">GO TO LOGIN</Link></div></section></Site>}
