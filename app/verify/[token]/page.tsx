export default async function Verify({params}:{params:Promise<{token:string}>}){const {token}=await params;return <main className="shell"><section className="card narrow"><span className="eyebrow">GLDC DOCUMENT VERIFICATION</span><h1>Document authenticity</h1><p className="muted">Verification reference: <b>{token}</b></p><p>This page is the online verification destination encoded in GLDC project PDFs. Use the verification API to validate the current status and document hash.</p><a className="btn" href={`/api/verify/${encodeURIComponent(token)}`}>CHECK LIVE RECORD</a></section></main>}
